#!/bin/zsh

source "$HOME/.config/zsh/bin/colors"

source ~/.zshrc
if command -v tmux >/dev/null 2>&1; then
  tmux list-panes -F '#{pane_pid}' | xargs -n1 kill -USR1 2>/dev/null
fi
colors green "[zshrc reloaded]"